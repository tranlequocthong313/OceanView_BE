version: '3.9'

services:
  mysql:
    image: mysql:latest
    volumes:
      - mysql:/var/lib/mysql
    env_file:
      - ./.env.container
  redis:
    image: redis:latest
    volumes:
      - redis:/data
    env_file:
      - ./.env.container
  api:
    build: .
    ports:
      - 8000:8000
    depends_on:
      - mysql
      - redis
    env_file:
      - ./.env.container

volumes:
  mysql: {}
  redis: {}
